{
  "lint": {
    "endpoints": {
      "schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "title": "Endpoint",
        "type": "object",
        "properties": {
          "@owner": { "type": "string" },
          "@group": { "type": "string" }
        },
        "required": ["@owner", "@group"]
      },
      "sort": {
        "by": ["@group", "endpoint", "method"]
      }
    }
  }
}
